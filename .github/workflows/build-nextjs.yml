name: Build Next
run-name: Build Next 🚀
on: [push]

permissions:
  id-token: write
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository code
        uses: actions/checkout@v3

      - name: use node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20.x'

      - name: Install dependencies
        run: npm install  --prefix source/frontend

      - name: Build Next.js application
        run: npm  --prefix source/frontend run build

      - name: List files
        run: ls -R 

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./source/frontend/out
      
      - uses: google-github-actions/setup-gcloud@v0
        with:
          service_account_email: ${{ secrets.GCP_SA_EMAIL }}
          service_account_key: $({ secrets.GCP_SA_KEY }}
          project_id: ${{ env.GCP_PROJECT_ID }}

      - name: Deploy via GCS
        run: |
          gsutil -m rm -rf gs://new-fortress-energy.appspot.com/* 
          gsutil -m cp -r ./source/frontend/* gs://new-fortress-energy.appspot.com/